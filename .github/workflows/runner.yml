name: Test Self-Hosted Runner

on:
  push:
    branches: [ main ]

jobs:
  deploy-to-aws:
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: List out files
        run: ls -la
      - name: Check runner
        run: pm2 restart app